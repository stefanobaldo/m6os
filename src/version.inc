; The version the product reports, in its banner. CHANGELOG.md's headings
; are checked against it by tools/check-version.sh: 0.0.0 is the version
; before the first tag and is never a heading; anything else must be one.
        define M6_VERSION "0.0.0"
